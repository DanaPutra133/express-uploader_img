# docker-compose.yml
version: '3.8' 

services:
  uploader-api:
    build: .
    container_name: uploader_container
    ports:
      - "3000:3000" 
    environment:
      - INTERNAL_API_KEY=${INTERNAL_API_KEY}